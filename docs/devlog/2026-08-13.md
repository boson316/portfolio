# 工作日誌 · 2026-08-13（portfolio / 小boson）

**專案：** [boson316/portfolio](https://github.com/boson316/portfolio)  
**Live：** https://boson316.github.io/portfolio/  
**主題：** Perxona 3D 整合、Groq 文字聊天、作品集內容更新、數位名片擴充

---

## 今日成果概覽

| 區塊 | 做了什麼 | 狀態 |
|------|----------|------|
| 架構 | `perxona` 後端併入 `portfolio/api` monorepo | ✅ push |
| 3D Avatar | 雙 FAB、Live iframe 預載 v13→v15、native.zip→import.zip 改寫 | ✅ push |
| 文字聊天 | Groq chip 短句、SYSTEM_PROMPT 去品牌腔 | ✅ push |
| 認證 | server-side proxy + CORS preflight、403 網域白名單 | ✅ push |
| 內容 | AWS 黑客松 + Workshop 證書上傳 | ✅ push |
| 數位名片 | FB / IG / LinkedIn / 雙 YT / 方格子 | ✅ push |
| 文件 | README 雙語、PERXONA.md §14–§17 | ✅ push |
| 知識庫 | 連結對照表防 LLM 幻覺 repo | ⚠️ 本地已改，待 push |

---

## 時間軸

### 上午 11:00–12:00 — 架構與雙 FAB

- **`e77657f`** 將 Perxona 後端併入 `api/` monorepo（Render Root Directory: `api`）
- **`fff48ee`** pre-push hook 放行 `.env.example`
- **`ce3ac87` → `b2b2f0c`** 文字聊天（💬 Groq）與 3D 小boson 分離；試 bubble 後恢復**雙 FAB**（紫 3D + 橘 💬）
- **`2fa74de` ~ `3148049`** session_token 修正、403 白名單提示、Live 連結改 `littleboson`、延遲 mount、`updateSessionToken` 不 abort、chip 固定短句

### 下午 13:00–15:30 — 403 排查與 proxy 繞過

- **`ccee4e5`** PERXONA.md §14 403 排查紀錄
- **`8d741a7`** JWT initialize code **1002** → 改走 **server-side `x-api-key` proxy**，失敗 fallback Live iframe
- **`ac145fb`** 新增 AWS 黑客松、Workshop 證書（assets + lightbox）
- **`0729995` ~ `bab6818`** proxy CORS preflight、disconnect debounce、error banner、timeout 120s
- 根因：**CDN `native.zip` 403**（同 rev 的 `import.zip` 200）→ fetch hook 改寫 + 預設改 Live iframe（v13）

### 下午 15:30–16:25 — 載入優化 + 文件

- **`f1c1a36`** v13：背景預載 Live iframe、grace 2s
- **`c062366`** README 雙語、PERXONA.md、知識庫、`api/app.py` 連結規則
- **`4a8a43f`** v14：全尺寸預載（避免 1×1 被節流）、載入殼、`preconnect`

### 下午 16:25–16:37 — 數位名片 + v15

- **`e489318`** 數位名片新增社群：FB、IG、LinkedIn、YouTube（生活 `@boson0777`、財金 `@Boson0727`）、方格子 `@Boson`
- **`1bd0a25`** v15：進站即掛 `#perxonaMount` 預載（非離屏 hidden）、`<head>` prefetch
- **`2203fc5`** README 同步數位名片社群連結

### 傍晚 16:18–16:37 — 對話品質調教（進行中）

- 3D 開場白仍偏英文 brand narrative → 指引改 Dashboard **Greeting**（非「指令」框）
- 實測 3D 給錯連結 `gpu-lab`（不存在）→ 正確為 `RTX3050-GPU-Mastery`
- **本地未 push：** `SYSTEM_PROMPT` + 知識庫 `.md/.txt` 新增**連結對照表** + intent test

---

## 問題 → 處置（精簡）

| 現象 | 根因 | 處置 |
|------|------|------|
| SDK toast「服務暫時無法使用」 | motion `native.zip` 403 | 預設 Live iframe + import.zip 改寫 |
| 按 3D 等 10–15s 空白 | SDK grace + Live 3D 資源重 | v14/v15 預載 + 載入殼 + preconnect |
| initialize code 1002 | Perxona 雲端 session key 未 provision | proxy `x-api-key` 繞過 |
| 3D 答「GPU Lab 連結」卻給假 repo | LLM 猜 repo 名 | 連結對照表（本地） |
| 回覆「極具潛力」「品牌敘事」 | Storyboard 舊模板 + Greeting 英文 | Dashboard 人工改（待做） |
| 💬 與 3D 知識不同步 | 兩套來源 | `SYSTEM_PROMPT` ↔ `knowledge-base.txt` 對齊 |

---

## Git 統計

- **26 commits**（11:04 → 16:36）
- **已 push `main`**
- **工作區未 commit（4 檔）：** 連結對照表 + `test_prompt_link_registry_forbids_hallucinated_gpu_repo`

---

## embed 版本演進

| 版本 | 重點 |
|------|------|
| v13 | 預設 Live iframe + 背景預載 |
| v14 | 全尺寸預載 + 載入殼 + preconnect |
| **v15** | 進站即掛 panel 內預載 + head prefetch |

---

## 待辦（人操 · 非 code）

- [ ] push 連結對照表 commit + Render redeploy
- [ ] Dashboard **重傳** `perxona-knowledge-base.txt` → **已發布**
- [ ] Dashboard **Greeting** 改繁中：「嗨，我是小boson…521×、ML 94.7%…」
- [ ] Storyboard 三節點去品牌顧問腔
- [ ] Perxona ticket：`native.zip` CDN 缺檔

---

## 驗證

```powershell
cd api; python -m pytest -q   # 19 passed（含新 link registry test，commit 後）
```

---

完整技術細節見 [`PERXONA.md`](../../PERXONA.md) §14–§17。
